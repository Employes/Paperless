import { Meta, Story, Canvas, Primary, ArgsTable } from '@storybook/addon-docs';
import { StoryCategories } from '../../categories';
import IconDocs from '../../../packages/core/src/components/atoms/icon/readme.md';
import { categorized, icons } from '../../../packages/core/src/utils/icons';

<Meta title={`${StoryCategories.Atoms}/Rebranding/Icon`} />

<IconDocs />

# Examples

## Variants

### Categorized

<Canvas>
	<div className='flex flex-col gap-12'>
		{Object.keys(categorized).map(category => (
			<div
				className='flex flex-col gap-6'
				key={category}
			>
				<h2>{category}</h2>
				<div className='flex w-full flex-wrap gap-4'>
					{Object.keys(categorized[category]).map(variant => (
						<p-tooltip
							content={variant}
							key={variant}
						>
							<p-icon
								key={variant}
								variant={variant}
								className='text-2xl text-black-teal hover:scale-125 hover:text-supportive-lilac'
								slot='trigger'
							></p-icon>
						</p-tooltip>
					))}
				</div>
			</div>
		))}
	</div>
</Canvas>

### List

<Canvas>
	<table className='w-full table-fixed'>
		<thead>
			<tr>
				<th className='py-2 text-start'>Icon</th>
				<th className='py-2 text-start'>Variant</th>
			</tr>
		</thead>
		<tbody>
			{Object.keys(icons).map(icon => (
				<tr
					className='group border-b border-b-mystic-dark'
					key={icon}
				>
					<td className='py-2'>
						<p-icon
							key={icon}
							variant={icon}
							className='text-2xl text-black-teal group-hover:text-supportive-lilac'
						></p-icon>
					</td>
					<td className='py-2 group-hover:font-semibold group-hover:text-supportive-lilac'>
						{icon}
					</td>
				</tr>
			))}
		</tbody>
	</table>
</Canvas>

### Flipping

#### Horizontal

<Canvas>
	<div className='flex'>
		<p-icon
			variant='arrow'
			flip='horizontal'
		/>
	</div>
</Canvas>

#### Vertical

<Canvas>
	<div className='flex'>
		<p-icon
			variant='user'
			flip='vertical'
		/>
	</div>
</Canvas>

### Rotation

#### 45 degrees

<Canvas>
	<div className='flex'>
		<p-icon
			variant='arrow'
			rotate={45}
		/>
	</div>
</Canvas>

#### -25 degrees

<Canvas>
	<div className='flex'>
		<p-icon
			variant='arrow'
			rotate={-25}
		/>
	</div>
</Canvas>
