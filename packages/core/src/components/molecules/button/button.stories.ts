import { html } from 'lit';

const meta = {
	title: 'Design System/Molecules/Button',
	component: 'p-button',
	args: {
		label: 'Button',
	},
};

export default meta;

export const Default = {
	tags: ['!dev'],
};
