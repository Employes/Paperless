import { html, nothing } from 'lit';

const meta = {
	title: 'Design System/Atoms/Smile',
	component: 'p-smile',
};

export default meta;

export const Default = {
	render: () => html`<p-smile />`,
	tags: ['!dev'],
};
